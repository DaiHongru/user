<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.freework.user.dao.NewsDao">
    <insert id="insert" useGeneratedKeys="true"
            keyProperty="newsId" keyColumn="newsId">
        INSERT INTO
        tb_news(
        owner_type,
        owner_id,
        status,
        content,
        create_time,
        last_edit_time
        ) VALUES (
        #{ownerType},
        #{ownerId},
        #{status},
        #{content},
        #{createTime},
        #{lastEditTime}
        )
    </insert>

    <select id="queryByRequirement" parameterType="com.freework.common.loadon.entity.News"
            resultType="com.freework.common.loadon.entity.News">
        SELECT
        news_id,
        owner_type,
        owner_id,
        status,
        content,
        create_time,
        last_edit_time
        FROM
        tb_news
        <where>
            <if test="newsId != null">
                AND news_id=#{newsId}
            </if>
            <if test="ownerId != null">
                AND owner_id=#{ownerId}
            </if>
            <if test="ownerType != null">
                AND owner_type=#{ownerType}
            </if>
            <if test="status != null">
                AND status=#{status}
            </if>
        </where>
        ORDER BY create_time DESC
    </select>

    <update id="update" parameterType="com.freework.common.loadon.entity.News">
        UPDATE
        tb_news
        SET
        status=#{status},
        last_edit_time=#{lastEditTime}
        WHERE
        news_id=#{newsId}
        AND owner_type=#{ownerType}
        AND owner_id=#{ownerId}
    </update>
</mapper>